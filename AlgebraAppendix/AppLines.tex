\documentclass{ximera}

\begin{document}
	\author{Stitz-Zeager}
	\xmtitle{Graphing Lines}{}


\mfpicnumber{1}

\opengraphsfile{AppLines}

\setcounter{footnote}{0}

\setlength{\extrarowheight}{2pt}

\label{AppLines}
 
In Section \ref{Distance}, we concerned ourselves with the finite line segment between two points $P$ and $Q$.  Specifically, we found its length (the distance between $P$ and $Q$) and its midpoint.  In this section, our focus will be on the \textit{entire} line, and ways to describe it algebraically.  Consider the generic situation below.

\begin{center}
    \begin{tikzpicture}[scale=0.7]

\coordinate (P) at (1,3);
\coordinate (Q) at (3,1);

\draw[<->,line width=1pt]
  ($(P)!-0.5!(Q)$) -- ($(P)!1.5!(Q)$);

\fill (P) circle (3pt);
\fill (Q) circle (3pt);

\node[above right] at (P) {\small $P(x_0,y_0)$};
\node[above right] at (Q) {\small $Q(x_1,y_1)$};

\end{tikzpicture}
\end{center}


To give a sense of the `steepness' of the line, we recall that we can compute the \textbf{slope} of the line as follows. (Read the character $\Delta$ as `change in'.)

 

%% \colorbox{ResultColor}{\bbm

% 

\begin{formula}\label{slope} The \index{slope ! definition} \textbf{slope} $m$ of the line containing the points $P\left(x_0, y_0\right)$ and $Q\left(x_1, y_1\right)$ is: \index{line ! slope of} \index{slope ! of a line}

\[ m  = \frac{y_1 - y_0}{x_1 - x_0} = \frac{\Delta y}{\Delta x},\]

provided $x_1 \neq x_0$, that is, $\Delta x \neq 0$.

\end{formula}

%% \ebm}

 

A couple of notes about Equation \ref{slope} are in order.  First, don't ask why we use the letter `$m$' to represent slope.  There are many explanations out there, but apparently no one really knows for sure.\footnote{See  \href{http://mathforum.org/dr.math/faq/faq.terms.html}{\underline{www.mathforum.org}} or \href{http://mathworld.wolfram.com/Slope.html}{\underline{www.mathworld.wolfram.com}} for discussions on this topic.} Secondly, the stipulation  $x_1 \neq x_0$ (or $\Delta x \neq 0$) ensures that we aren't trying to divide by zero.  The reader is invited to pause to think about what is happening geometrically when the `change in $x$' is $0$; the anxious reader can skip along to the next example.

\begin{example}  Find the slope of the line containing the following pairs of points, if it exists.  Plot each pair of points and the line containing them.


\begin{enumerate}

\item  $P(0,0)$, $Q(2,4)$
\item  $P(-1,2)$, $Q(3,4)$

\item  $P(-2,3)$, $Q(2,-3)$
\item  $P(-3,2)$, $Q(4,2)$


\item  $P(2,3)$, $Q(2,-1)$
\item  $P(2,3)$, $Q(2.1, -1)$

\end{enumerate}

\begin{explanation}
In each of these examples, we apply the slope formula, Equation \ref{slope}.

\begin{enumerate}

\item   

$m = \frac{4-0}{2-0} = \frac{4}{2} = 2$


\begin{tikzpicture}[scale=0.5]
\draw[->] (-5,0) -- (5,0) node[right] {$x$};
\draw[->] (0,-5) -- (0,5) node[above] {$y$};
\foreach \x in {-4,-3,-2,-1,1,2,3,4} {
  \draw (\x,0.15) -- (\x,-0.15);
  \node[below] at (\x,-0.15) {$\x$};
}
\foreach \y in {-4,-3,-2,-1,1,2,3,4} {
  \draw (0.15,\y) -- (-0.15,\y);
  \node[left] at (-0.15,\y) {$\y$};
}

\coordinate (P) at (0,0);
\coordinate (Q) at (2,4);

\draw[<->,line width=1pt]
  ($(P)!-0.5!(Q)$) -- ($(P)!1.5!(Q)$);

\fill (P) circle (3pt);
\fill (Q) circle (3pt);

\node[above right] at (P) {\small $P(0,0)$};
\node[above right] at (Q) {\small $Q(2,4)$};

\end{tikzpicture}



\item 

$m = \frac{4 - 2}{3 - (-1)} = \frac{2}{4} = \frac{1}{2}$


\begin{tikzpicture}[scale=0.5]
\draw[->] (-5,0) -- (5,0) node[right] {$x$};
\draw[->] (0,-5) -- (0,5) node[above] {$y$};
\foreach \x in {-4,-3,-2,-1,1,2,3,4} {
  \draw (\x,0.15) -- (\x,-0.15);
  \node[below] at (\x,-0.15) {$\x$};
}
\foreach \y in {-4,-3,-2,-1,1,2,3,4} {
  \draw (0.15,\y) -- (-0.15,\y);
  \node[left] at (-0.15,\y) {$\y$};
}

\coordinate (P) at (-1,2);
\coordinate (Q) at (3,4);

\draw[<->,line width=1pt]
  ($(P)!-0.5!(Q)$) -- ($(P)!1.5!(Q)$);

\fill (P) circle (3pt);
\fill (Q) circle (3pt);

\node[above left] at (P) {\small $P(-1,2)$};
\node[above left] at (Q) {\small $Q(3,4)$};

\end{tikzpicture}

\item

$ m = \frac{-3 - 3}{2 - (-2)} = \frac{-6}{4} = -\frac{3}{2}$ 

\begin{tikzpicture}[scale=0.5]
\draw[->] (-5,0) -- (5,0) node[right] {$x$};
\draw[->] (0,-5) -- (0,5) node[above] {$y$};
\foreach \x in {-4,-3,-2,-1,1,2,3,4} {
  \draw (\x,0.15) -- (\x,-0.15);
  \node[below] at (\x,-0.15) {$\x$};
}
\foreach \y in {-4,-3,-2,-1,1,2,3,4} {
  \draw (0.15,\y) -- (-0.15,\y);
  \node[left] at (-0.15,\y) {$\y$};
}

\coordinate (P) at (-2,3);
\coordinate (Q) at (2,-3);

\draw[<->,line width=1pt]
  ($(P)!-0.5!(Q)$) -- ($(P)!1.5!(Q)$);

\fill (P) circle (3pt);
\fill (Q) circle (3pt);

\node[below left] at (P) {\small $P(-2,3)$};
\node[above right] at (Q) {\small $Q(2,-3)$};

\end{tikzpicture} 

 

\item   $ m = \frac{2 - 2}{4 - (-3)} = \frac{0}{7} = 0$ 

\begin{tikzpicture}[scale=0.5]
\draw[->] (-5,0) -- (5,0) node[right] {$x$};
\draw[->] (0,-5) -- (0,5) node[above] {$y$};
\foreach \x in {-4,-3,-2,-1,1,2,3,4} {
  \draw (\x,0.15) -- (\x,-0.15);
  \node[below] at (\x,-0.15) {$\x$};
}
\foreach \y in {-4,-3,-2,-1,1,2,3,4} {
  \draw (0.15,\y) -- (-0.15,\y);
  \node[left] at (-0.15,\y) {$\y$};
}

\coordinate (P) at (-3,2);
\coordinate (Q) at (4,2);

\draw[<->,line width=1pt]
  ($(P)!-0.5!(Q)$) -- ($(P)!1.5!(Q)$);

\fill (P) circle (3pt);
\fill (Q) circle (3pt);

\node[above] at (P) {\small $P(-3,2)$};
\node[above] at (Q) {\small $Q(4,2)$};

\end{tikzpicture} 

\item

$ m = \frac{-1 - 3}{2 - 2} = \frac{-4}{0}$, which is undefined 

\begin{tikzpicture}[scale=0.5]
\draw[->] (-5,0) -- (5,0) node[right] {$x$};
\draw[->] (0,-5) -- (0,5) node[above] {$y$};
\foreach \x in {-4,-3,-2,-1,1,2,3,4} {
  \draw (\x,0.15) -- (\x,-0.15);
  \node[below] at (\x,-0.15) {$\x$};
}
\foreach \y in {-4,-3,-2,-1,1,2,3,4} {
  \draw (0.15,\y) -- (-0.15,\y);
  \node[left] at (-0.15,\y) {$\y$};
}

\coordinate (P) at (2,3);
\coordinate (Q) at (2,-1);

\draw[<->,line width=1pt]
  ($(P)!-0.5!(Q)$) -- ($(P)!1.5!(Q)$);

\fill (P) circle (3pt);
\fill (Q) circle (3pt);

\node[right] at (P) {\small $P(2,3)$};
\node[below right] at (Q) {\small $Q(2,-1)$};

\end{tikzpicture}

\item

$ m = \frac{-1 - 3}{2.1 - 2} = \frac{-4}{0.1}=-40$ 

\begin{tikzpicture}[scale=0.5]
\draw[->] (-5,0) -- (5,0) node[right] {$x$};
\draw[->] (0,-5) -- (0,5) node[above] {$y$};
\foreach \x in {-4,-3,-2,-1,1,2,3,4} {
  \draw (\x,0.15) -- (\x,-0.15);
  \node[below] at (\x,-0.15) {$\x$};
}
\foreach \y in {-4,-3,-2,-1,1,2,3,4} {
  \draw (0.15,\y) -- (-0.15,\y);
  \node[left] at (-0.15,\y) {$\y$};
}

\coordinate (P) at (2,3);
\coordinate (Q) at (2.1,-1);

\draw[<->,line width=1pt]
  ($(P)!-0.5!(Q)$) -- ($(P)!1.5!(Q)$);

\fill (P) circle (3pt);
\fill (Q) circle (3pt);

\node[right] at (P) {\small $P(2,3)$};
\node[below right] at (Q) {\small $Q(2.1,-1)$};

\end{tikzpicture}

\end{enumerate}

\label{slopeex}



\end{explanation}

\end{example} 


A few comments about Example \ref{slopeex} are in order.  First, if the slope is positive then the resulting line is said to be `increasing', meaning as we move from left to right,\footnote{That is, as we increase the $x$-values \ldots} the $y$-values are getting larger.\footnote{We'll have more to say about this idea in Section \ref{LinearFunctions}.}  Similarly, if the slope is negative, we say the line is `decreasing', since as we move from left to right, the $y$-values are getting smaller.  A slope of $0$ results in a horizontal line which we say is `constant', since the $y$-values here remain unchanged as we move from left to right,  and an undefined slope results in a vertical line.\footnote{Some authors use the unfortunate moniker `no slope' when a slope is undefined.  It's easy to confuse the notions of `no slope' with `slope of $0$'.  For this reason, we will describe slopes of vertical lines as `undefined'.}   


Second, the larger the slope is in absolute value, the steeper the line.  You may recall from Intermediate Algebra that slope can be described as the ratio `$\frac{\text{rise}}{\text{run}}$'.  For example, if the slope works out to be $\frac{1}{2}$, we can interpret this as a `rise' of $1$ unit upward for every `run' of $2$ units to the right:

\begin{center}
\begin{tikzpicture}[scale=1.1]

% Axes
\draw[->] (-2,0) -- (4.5,0) node[right] {\scriptsize $x$};
\draw[->] (0,0) -- (0,5.5) node[above] {\scriptsize $y$};

% Axis ticks and labels
\foreach \x in {-1,1,2,3}
  \draw (\x,0.1) -- (\x,-0.1) node[below] {\tiny $\x$};

\foreach \y in {1,2,3,4}
  \draw (0.1,\y) -- (-0.1,\y) node[left] {\tiny $\y$};

% Main arrow (trend line)
\draw[<->, thick] (-2,1.5) -- (4,4.5);

% Dashed step path
\draw[dashed]
  (-1,2) -- (1,2) -- (1,3) -- (3,3) -- (3,4);

% Points
\foreach \x/\y in {-1/2, 1/3, 3/4}
  \fill (\x,\y) circle (3pt);

% Labels
\node at (2,2.75) {over $2$};
\node[above] at (3.5,3.35) {up $1$};

\end{tikzpicture}
\end{center}

In this way, we may view the slope as  `the \textbf{rate of change} of $y$ with respect to $x$'.  From the expression \[ m = \frac{\Delta y}{\Delta x}\] we get $\Delta y = m \Delta x$ so that the $y$-values change `$m$' times as fast as the $x$-values.  We'll have more to say about this concept in Section \ref{LinearFunctions} when we explore applications of linear functions;  presently, we will keep our attention focused on the analytic geometry of lines.  To that end, our next task is to find algebraic equations that describe lines and we start with a discussion of vertical and horizontal lines.


Consider the two lines shown below: $V$ (for `V'ertical Line) and $H$ (for `H'orizontal Line).    



\begin{center}
\begin{tikzpicture}[scale=0.5,every node/.style={font=\scriptsize}]

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Left figure: Line V
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{scope}

% Axes
\draw[->] (-1.2,0) -- (5.2,0);
\draw[->] (0,-5.2) -- (0,5.2);

\node[right] at (5.1,0) {$x$};
\node[above] at (0,5.1) {$y$};

% Tick marks and labels
\foreach \x in {1,2,3,4}{
  \draw (\x,0.08)--(\x,-0.08);
  \node[below] at (\x,0) {$\x$};
}

\foreach \y in {-4,-3,-2,-1,1,2,3,4}{
  \draw (0.08,\y)--(-0.08,\y);
  \node[left] at (0,\y) {$\y$};
}

% Vertical line x=3
\draw[<->,line width=1pt] (3,-5.2) -- (3,5.2);

% Caption
\node[font=\normalsize] at (2,-6.2) {The line $V$};

\end{scope}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Right figure: Line H
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{scope}[xshift=15cm]

% Axes
\draw[->] (-5.2,0) -- (5.2,0);
\draw[->] (0,-5.2) -- (0,1.2);

\node[right] at (5.1,0) {$x$};
\node[above] at (0,1.1) {$y$};

% Tick marks and labels
\foreach \x in {-4,-3,-2,-1,1,2,3,4}{
  \draw (\x,0.08)--(\x,-0.08);
  \node[below] at (\x,0) {$\x$};
}

\foreach \y in {-4,-3,-2,-1}{
  \draw (0.08,\y)--(-0.08,\y);
  \node[left] at (0,\y) {$\y$};
}

% Horizontal line y=-2
\draw[<->,line width=1pt] (-5.2,-2) -- (5.2,-2);

% Caption
\node[font=\normalsize] at (0,-6.2) {The line $H$};

\end{scope}

\end{tikzpicture}
\end{center}

 

All of the points on the line $V$ have an $x$-coordinate of $3$.  Conversely, any point with an $x$-coordinate of $3$ lies on the line $V$.  Said differently, the point $(x,y)$ lies on $V$ if and only if $x = 3$.  Because of this, we say the equation $x=3$ \textit{describes} the line $V$, or, said differently, the \textit{graph} of the equation $x=3$ is the line $V$.  

 

In Section \ref{Relations}, we'll spend a great deal of time talking about graphing equations.  For now, it suffices to know that a graph of an equation is a plot of all of the points which make the equation true.  So to graph $x=3$,  we plot all of the points $(x,y)$ which satisfy $x = 3$ and this gives us our vertical line $V$.      

 

Turning our attention to $H$, we note that every point on $H$ has a $y$-coordinate of $-2$, and vice-versa.  Hence the equation $y = -2$ describes the line $H$, or the graph of the equation $y=-2$ is $H$.  In general:

 

%% \colorbox{ResultColor}{\bbm

\begin{formula} \label{verticalhorizontallines} Equations of Vertical and Horizontal Lines

\begin{itemize}

\item The graph of the equation $x = a$ in the $xy$-plane is a \textbf{vertical line} through $(a, 0)$.\index{line ! vertical} \index{vertical line}

\item The graph of the equation $y = b$ in the $xy$-plane is a \textbf{horizontal line} through $(0, b)$.\index{line ! horizontal} \index{horizontal line}

\end{itemize}

\end{formula}

%% \ebm}

 

Of course, we may be working on axes which aren't labeled with the `usual' $x$'s and $y$'s.  In this case, we understand Equation \ref{verticalhorizontallines} to say `$\text{horizontal axis label} = a$' describes a \textit{vertical} line through $(a,0)$ and  `$\text{vertical axis label} = b$' describes a \textit{horizontal} line through $(0,b)$.  


\begin{example}  \label{horizontalverticallineex}  $~$

\begin{enumerate}  \item Graph the following equations in the $xy$-plane: 

\begin{enumerate}

\item $y = 3$ 

\item $x=-117$

\end{enumerate}

\item Find the equation of each of the given lines.

\begin{center}
\begin{tikzpicture}[scale=0.75,every node/.style={font=\scriptsize}]

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Left figure: Line L1
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{scope}

% Axes
\draw[->] (-1.2,0) -- (5.2,0);
\draw[->] (0,-5.2) -- (0,5.2);

\node[right] at (5.1,0) {$x$};
\node[above] at (0,5.1) {$y$};

% Tick marks and labels
\foreach \x in {1,2,3,4}{
    \draw (\x,0.08)--(\x,-0.08);
    \node[below] at (\x,0) {$\x$};
}

\foreach \y in {-4,-3,-2,-1,1,2,3,4}{
    \draw (0.08,\y)--(-0.08,\y);
    \node[left] at (0,\y) {$\y$};
}

% Line x = 2
\draw[<->,line width=1pt] (2,-5.2) -- (2,5.2);

% Caption
\node[font=\normalsize] at (2,-6.2) {Line $L_1$};

\end{scope}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Right figure: Line L2
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{scope}[xshift=10cm]

% Axes
\draw[->] (-5.2,0) -- (5.2,0);
\draw[->] (0,-1.2) -- (0,5.2);

\node[right] at (5.1,0) {$t$};
\node[above] at (0,5.1) {$s$};

% Tick marks and labels
\foreach \x in {-4,-3,-2,-1,1,2,3,4}{
    \draw (\x,0.08)--(\x,-0.08);
    \node[below] at (\x,0) {$\x$};
}

\foreach \y in {1,2,3,4}{
    \draw (0.08,\y)--(-0.08,\y);
    \node[left] at (0,\y) {$\y$};
}

% Line y = 3
\draw[<->,line width=1pt] (-5.2,3) -- (5.2,3);

% Caption
\node[font=\normalsize] at (0,-2.2) {Line $L_2$};

\end{scope}

\end{tikzpicture}
\end{center}

\end{enumerate}

\begin{explanation}
    
\begin{enumerate}

\item  Since we're in the familiar $xy$-plane, the graph of $y=3$ is a horizontal line through $(0,3)$, shown below on the left and the graph of $x = -117$ is a vertical line through $(-117, 0)$.  We scale the $x$-axis differently than the $y$-axis to produce the graph below on the right.

\begin{center}
\begin{tikzpicture}[scale=0.5,every node/.style={font=\scriptsize}]

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Left figure: y = 3
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{scope}

% Axes
\draw[->] (-5.2,0) -- (5.2,0);
\draw[->] (0,-1.2) -- (0,5.2);

\node[right] at (5.1,0) {$x$};
\node[above] at (0,5.1) {$y$};

% Tick marks and labels
\foreach \x in {-4,-3,-2,-1,1,2,3,4}{
  \draw (\x,0.08)--(\x,-0.08);
  \node[below] at (\x,0) {$\x$};
}

\foreach \y in {1,2,3,4}{
  \draw (0.08,\y)--(-0.08,\y);
  \node[left] at (0,\y) {$\y$};
}

% Horizontal line y=3
\draw[<->,line width=1pt] (-5.2,3) -- (5.2,3);

% Point and label
\fill (0,3) circle (3pt);
\node[above right] at (0,3) {$(0,3)$};

% Caption
\node[font=\normalsize] at (0,-2.1) {The line $y=3$};

\end{scope}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Right figure: x = -117
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{scope}[xshift=15cm]

% Axes
\draw[->] (-5.2,0) -- (1.2,0);
\draw[->] (0,-5.2) -- (0,5.2);

\node[right] at (1.1,0) {$x$};
\node[above] at (0,5.1) {$y$};

% x tick at displayed value -117
\draw (-3,0.08)--(-3,-0.08);
\node[below] at (-3,0) {$-117$};

% y ticks
\foreach \y in {-4,-3,-2,-1,1,2,3,4}{
  \draw (0.08,\y)--(-0.08,\y);
  \node[left] at (0,\y) {$\y$};
}

% Vertical line
\draw[<->,line width=1pt] (-3,-5.2) -- (-3,5.2);

% Point and label
\fill (-3,0) circle (3pt);
\node[above right] at (-3,0) {$(-117,0)$};

% Caption
\node[font=\normalsize] at (-2,-6.2) {The line $x=-117$};

\end{scope}

\end{tikzpicture}
\end{center}

\item  Since $L_{1}$ is a vertical line through $(2,0)$, and the horizontal axis is labeled with `$x$', the equation of $L_{1}$ is $x = 2$.  Since $L_{2}$ is a horizontal line through $(0,3)$ and the vertical axis is labeled as `$s$', the equation of this line is $s = 3$.  \qed

\end{enumerate}
\end{explanation}
\end{example}


Using the concept of slope, we can develop equations for the other varieties of lines.  Suppose a line has a slope of $m$ and contains the point $\left(x_0, y_0\right)$.  Suppose $(x,y)$ is another point on the line, as indicated below.

\begin{center}
\begin{tikzpicture}[scale=0.9]

% Coordinates
\coordinate (P) at (-1,2);
\coordinate (Q) at (3,4);

% Line through the two points
\draw[<->,line width=1pt] (-2,1.5) -- (4,4.5);

% Dashed helper lines
\draw[dashed] (P) -- (3,2) -- (Q);

% Points
\fill (P) circle (3pt);
\fill (Q) circle (3pt);

% Labels
\node[above left] at (P) {$\left(x_0,y_0\right)$};
\node[above left] at (Q) {$\left(x,y\right)$};

\end{tikzpicture}
\end{center}

Equation \ref{slope} yields

\vspace{-.1in}

\setlength{\extrarowheight}{2pt}

\[ \begin{array}{rclr}  
                      m & = & \frac{y - y_0}{x-x_0} & \\
m\left(x - x_0\right) & = & y - y_0 & \\ 
              y - y_0 & = & m\left(x - x_0\right) & \\
   \end{array} \]

which is known as the \textbf{point-slope form} of a line.

 

%% \colorbox{ResultColor}{\bbm

\begin{formula}\label{pointslope} The\index{point-slope form of a line}\index{line ! point-slope form} \textbf{point-slope form} of the line with slope $m$ containing the point $\left(x_0, y_0\right)$ is the equation \[y - y_0  =  m\left(x - x_0\right)\] $ $ \vspace{-.2in}
\end{formula}

%% \ebm}

 

A few remarks about Equation \ref{pointslope} are in order. First, note that if the slope $m = 0$, then the line is horizontal and Equation \ref{pointslope} reduces to  $y - y_0 = 0$ or $y = y_0$, as prescribed by Equation \ref{verticalhorizontallines}.\footnote{Here we have $y_0$ as the constant whereas in the Equation we used the letter $b$. The form $y =$ constant is what matters.} Second, we may need to change the letters in Equation \ref{pointslope} from `$x$' and `$y$' depending on the context, so while  Equation \ref{pointslope} should be committed to memory, it should be understood that `$x$' refers to whichever variable is used to label the horizontal axis, and $y$ refers to whichever variable is used to label the vertical axis. Lastly, while Equation \ref{pointslope} is, by far, the easiest way to \textit{construct} the equation of a line given a point and a slope, more often than not, the equation is solved for $y$ and simplified into the form below.

 

%% \colorbox{ResultColor}{\bbm

\begin{formula}\label{slopeintercept} The\index{slope-intercept form of a line}\index{line ! slope-intercept form} \textbf{slope-intercept form} of the line with slope $m$ and $y$-intercept $(0,b)$ is the equation \[y  =  mx + b\] $ $ \vspace{-.2in}
\end{formula}

%% \ebm}

 

Equation \ref{slopeintercept} is probably\footnote{Hopefully?} a familiar sight from Intermediate Algebra.  You may recall from that class that the  `intercept' in `slope-intercept' comes from the fact that this line `intercepts' or crosses the $y$-axis at the point $(0,b)$.\footnote{We can verify this algebraically by setting $x=0$ in the equation $y=mx+b$ and obtaining $y = b$.} If we set the slope, $m = 0$, we obtain $y=b$, the formula for Horizontal Lines first introduced in Equation \ref{verticalhorizontallines}.  Hence, any line which has a defined slope $m$ can be represented in both point-slope and slope-intercept forms.  The only exceptions are vertical lines.\footnote{We'll have more to say about this in Section \ref{LinearFunctions}.}  There is one equation - the aptly named 'general form' - which describes every type of line and it is presented next.


%% \colorbox{ResultColor}{\bbm

\begin{formula}\label{generallineform} Every line may be represented by an equation of the form $Ax + By = C$, where $A$, $B$ and $C$ are real numbers for which $A$ and $B$ aren't both zero.  This is called \textit{a}  \index{line ! general form}\textbf{general form} of the line. \index{general form of a line}
\end{formula}


Note the indefinite article `a' in Equation \ref{generallineform}.  The line $y = 5$ is a general form for the horizontal line through $(0,5)$, but so are $3y = 15$ and $0.5 y = 2.5$.  The reader is left to ponder the use of the definite article `the' in Equations \ref{pointslope} and \ref{slopeintercept}.  Regardless of \textit{which} form the equation of a line takes, note that the variables involved are all raised to the first power.\footnote{Recall, $x = x^{1}$, $y = y^{1}$, etc.}  For instance, there are no $\sqrt{x}$ terms, no $y^2$ terms or any variables appearing in denominators.  Let's look at a few examples.

\begin{example}  $~$

\begin{enumerate}

\item Graph the following equations in the $xy$-plane:

\begin{multicols}{2}
\begin{enumerate}

\item  $y = 3x - 1$

\item  $2x + 4y = 3$

\end{enumerate}
\end{multicols}

\item  Find the slope-intercept form of the line containing the points $(-1,3)$ and $(2,1)$.

\item  Find the slope-intercept form of the equation of the line below:

\begin{center}
\begin{tikzpicture}[scale=0.75,every node/.style={font=\scriptsize}]

% Axes
\draw[->] (-1.2,0) -- (6.2,0);
\draw[->] (0,-1.2) -- (0,6.2);

\node[right] at (6.1,0) {$t$};
\node[above] at (0,6.1) {$s$};

% Tick marks and labels
\foreach \x in {1,2,3,4,5}{
    \draw (\x,0.08)--(\x,-0.08);
    \node[below] at (\x,0) {\tiny $\x$};
}

\foreach \y in {1,2,3,4,5}{
    \draw (0.08,\y)--(-0.08,\y);
    \node[left] at (0,\y) {\tiny $\y$};
}

% Line
\draw[<->,line width=1pt] (-1,6) -- (6,-1);

% Points
\fill (0,5) circle (3pt);
\fill (5,0) circle (3pt);

\end{tikzpicture}
\end{center}

\end{enumerate}

 

{\bf Solution.}  

\begin{enumerate}

\item To graph a line, we need just two points on that line.  There are several ways to do this, and we showcase two of them here.  For the first equation, we recognize that $y = 3x-1$ is in slope-intercept form, $y = mx+b$, with $m = 3$ and $b = -1$.  This immediately gives us one point on the graph -- the $y$-intercept $(0,-1)$. From here, we use the slope $m = 3 = \frac{3}{1}$ and move one unit to the right and three units up, to obtain a second point on the line, $(1,2)$.  Connecting these points gives us the graph below on the left.  

 

The second equation,  $2x+4y = 3$, is a general form of a line.  To get two points here, we choose `convenient' values for one of the variables, and solve for the other variable.  Choosing $x=0$, for example, reduces $2x+4y = 3$ to $4y = 3$, or $y = \frac{3}{4}$.  This means the point $\left(0, \frac{3}{4} \right)$ is on the graph.  Choosing $y = 0$ gives $2x = 3$, or $x = \frac{3}{2}$.  This gives is a second point on the line, $\left(\frac{3}{2}, 0 \right)$.\footnote{You may recall, that this is the $x$-intercept of the line.} Our graph of $2x+4y = 3$ is below on the right.

\begin{center}
\begin{tikzpicture}[scale=0.75,every node/.style={font=\scriptsize}]

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Left graph: y = 3x - 1
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{scope}

% Axes
\draw[->] (-3.2,0) -- (3.2,0);
\draw[->] (0,-5.2) -- (0,5.2);

\node[right] at (3.1,0) {$x$};
\node[above] at (0,5.1) {$y$};

% Tick marks
\foreach \x in {-2,-1,1,2}{
  \draw (\x,0.08)--(\x,-0.08);
  \node[below] at (\x,0) {\tiny $\x$};
}

\foreach \y in {-4,-3,-2,-1,1,2,3,4}{
  \draw (0.08,\y)--(-0.08,\y);
}

% Label selected y-values (to match the original)
\foreach \y in {-1,1,2,3,4}{
  \node[left] at (0,\y) {\tiny $\y$};
}

% Line
\draw[<->,line width=1pt] (-1,-4) -- (2,5);

% Points
\fill (0,-1) circle (3pt);
\fill (1,2) circle (3pt);

% Caption
\node[font=\normalsize] at (0,-6.2) {$y=3x-1$};

\end{scope}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Right graph: 2x + 4y = 3
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{scope}[xshift=9cm]

% Axes
\draw[->] (-4.2,0) -- (4.2,0);
\draw[->] (0,-1.2) -- (0,3.2);

\node[right] at (4.1,0) {$x$};
\node[above] at (0,3.1) {$y$};

% Tick marks
\foreach \x in {-3,-2,-1,1,2,3}{
  \draw (\x,0.08)--(\x,-0.08);
  \node[below] at (\x,0) {\tiny $\x$};
}

\foreach \y in {1,2}{
  \draw (0.08,\y)--(-0.08,\y);
  \node[left] at (0,\y) {\tiny $\y$};
}

% Line
\draw[<->,line width=1pt] (-3,2.25) -- (3,-0.75);

% Points
\fill (0,0.75) circle (3pt);
\fill (1.5,0) circle (3pt);

% Caption
\node[font=\normalsize] at (0,-2.0) {$2x+4y=3$};

\end{scope}

\end{tikzpicture}
\end{center}

\item We'll assume we're using the familiar $(x,y)$ axis labels and begin by finding the slope of the line using Equation \ref{slope}:  $m = \frac{\Delta y}{\Delta x} = \frac{1 - 3}{2 - (-1)} = -\frac{2}{3}$.  Next, we substitute this result, along with one of the given points, into the point-slope equation of the line, Equation \ref{pointslope}. We have two options for the point $\left(x_0, y_0\right)$. We'll use $(-1,3)$ and leave it to the reader to check that using $(2,1)$ results in the same equation.  Substituting into the point-slope form of the line, we get 


\setlength{\extrarowheight}{10pt}
\[\begin{array}{rclr} 
y - y_0 & = & m\left(x - x_0\right)  & \\
y - 3 & = & -\frac{2}{3} \left(x - (-1)\right) & \\
y - 3 & = & -\frac{2}{3} \left(x +1 \right) & \\
y - 3& = & -\frac{2}{3}x - \frac{2}{3}\\
y& = & -\frac{2}{3}x - \frac{2}{3} + 3\\
y & = & -\frac{2}{3} x + \frac{7}{3}. \\ 
\end{array} \]

We can check our answer by showing that both $(-1,3)$ and $(2,1)$ are on the graph of $y  =  -\frac{2}{3} x + \frac{7}{3}$ algebraically by showing that the equation holds true when we substitute $x = -1$ and $y=3$ and when $x = 2$ and $y = 1$.

\item  From the graph, we see that the points $(0,5)$ and $(5,0)$ are on the line, so we may proceed as we did in the previous problem.  Here, however,  we use `$t$' in place of `$x$' and `$s$' in place of `$y$' in accordance to the axis labels given.  We find the slope $m = \frac{\Delta s}{\Delta t} = \frac{0 - 5}{5 - 0} = -1$.  As before, we have two points to choose from to substitute into the point-slope formula, and, as before, we'll select one of them, $(0,5)$ and leave the computations with $(5,0)$ to the reader.  

\[\begin{array}{rclr} 
s - s_0 & = & m\left(t - t_0\right)  & \\
s - 5 & = & (-1) \left(t -0 \right) & \\
s- 5 & = &-t & \\
s& = & -t + 5.\\
\end{array} \]

\enlargethispage{.1in}

As before we can check this line contains both points $(t,s) = (0,5)$ and $(t,s) = (5,0)$ algebraically.  \qed

\end{enumerate}

\end{example}

While every point on a line holds value and meaning,\footnote{Lines missing points  - even one - usually belie some algebraic pathology which we'll discuss in more detail in Chapter \ref{RationalFunctions}.} we've reminded you of certain points, called `intercepts,' which hold special enough significance to be singled out. Formally, we define these as follows.

 

%% \colorbox{ResultColor}{\bbm

% 

\begin{definition}  Given a graph of an equation in the $xy$-plane:

\label{interceptsdefn}

\begin{itemize}

\item  A point on a graph which is also on the $x$-axis is called an \index{$x$-intercept} \textbf{\boldmath $x$-intercept} of the graph.  To determine the $x$-intercept(s) of a graph, set $y = 0$ in the equation and solve for $x$.

\textbf{NOTE:}  $x$-intercepts always have the form:  $(x_0,0)$.

\item  A point on a graph which is also on the $y$-axis is called an \index{$y$-intercept} \textbf{\boldmath $y$-intercept} of the graph. To determine the $y$-intercept(s) of a graph, set $x = 0$ in the equation and solve for $y$.

\textbf{NOTE:}  $y$-intercepts always have the form:  $(0,y_0)$.

\end{itemize}

\end{definition}

%% \ebm}

 

As usual, the labels of the axes in the problem will dictate the labels on the intercepts.  If we're working in the $vw$-plane, for instance, there would be $v$- and $w$-intercepts.

 

The last little bit of analytic geometry we need to review about lines are the concepts of `parallel' and `perpendicular' lines.  Parallel lines do not intersect,\footnote{Well, at least in Euclidean Geometry \dots} and hence, parallel lines necessarily have the same slope.  Perpendicular lines intersect at a right ($90^{\circ}$) angle.   The relationship between these slopes is somewhat more complicated, and is summarized below.


%% \colorbox{ResultColor}{\bbm

% 

\begin{theorem}  Suppose line $L_1$ has slope $m_1$ and line $L_2$ has slope $m_2$:


\label{parallelperpendicularslopetheorem}


\begin{itemize}

\item $L_1$ and $L_2$ are parallel (written $L_1 \parallel L_2$) if and only if $m_1 = m_2$.

\item If $m_1 \neq 0$ and  $m_2 \neq 0$ then $L_1$ and $L_2$ are perpendicular (written $L_1 \perp L_2$) if and only if $m_1 m_2 = -1$.

\textbf{NOTE:} $m_1 m_2 = -1$  is equivalent to $m_2 = -\frac{1}{m_1}$, so that perpendicular lines have slopes which are `opposite reciprocals' of one another.

\end{itemize}

\end{theorem}

%% \ebm}

\begin{center}
\begin{tikzpicture}[scale=0.75,every node/.style={font=\scriptsize}]

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Left figure: Parallel lines
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{scope}

% Axes
\draw[->] (-1.2,0) -- (5.2,0);
\draw[->] (0,-1.2) -- (0,5.2);

\node[right] at (5.1,0) {$x$};
\node[above] at (0,5.1) {$y$};

% Lines
\draw[<->,line width=1pt] (-1,-1) -- (4,4);
\draw[<->,line width=1pt] (-2,0) -- (3,5);

% Labels
\node at (4.4,4.0) {$L_1$};
\node at (3.4,5.0) {$L_2$};

% Caption
\node[font=\normalsize,align=center] at (2,-2)
{$L_1\parallel L_2,\quad m_1=m_2$};

\end{scope}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Right figure: Perpendicular lines
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{scope}[xshift=10cm]

% Axes
\draw[->] (-1.2,0) -- (5.2,0);
\draw[->] (0,-1.2) -- (0,5.2);

\node[right] at (5.1,0) {$x$};
\node[above] at (0,5.1) {$y$};

% Lines
\draw[<->,line width=1pt] (-1,-1) -- (4,4);
\draw[<->,line width=1pt] (-1,5) -- (4.5,-0.5);

% Intersection point
\fill (2,2) circle (3pt);

% Right-angle marker
\draw (2,2) -- (1.75,2.25) -- (2,2.5) -- (2.25,2.25);

% Labels
\node at (4.4,4.0) {$L_1$};
\node at (5.0,-1.0) {$L_2$};

% Caption
\node[font=\normalsize,align=center] at (2,-2)
{$L_1\perp L_2,\quad m_1m_2=-1$};

\end{scope}

\end{tikzpicture}
\end{center}


A few remarks about Theorem \ref{parallelperpendicularslopetheorem} are in order.  First off, the theorem assumes that the slopes of the lines exist.  The reader is encouraged to think about the case when one (or both) of the slopes don't exist.  Along those same lines, the reader is encouraged to think about why the stipulations $m_1 \neq 0$ and $m_2 \neq 0$ appear in the statement regarding slopes of perpendicular lines, and what happens in this case as well.  (Think geometrically!)   In Exercise \ref{perpendicularlineproof}, you'll prove the assertion about the slopes of perpendicular lines.  For now, we accept it as true and use it in the following example.

\begin{example}  For line $y = 2x-1$ and the point $(3,4)$, find:

\begin{enumerate}

\item the equation of the line parallel to the given line which contains the given point.

\item   the equation of the line perpendicular to the given line which contains the given point.  Check your answers by graphing them, along with the original line, using a graphing utility.

\end{enumerate}

{\bf Solution.}

\begin{enumerate}

\item Since $y = 2x-1$ is already in slope-intercept form, we have the slope $m = 2$.  To find the line parallel to this line containing $(3,4)$, we use the point-slope form with $m = 2$ to get: \[\begin{array}{rclr} 
y - y_0 & = & m\left(x - x_0\right)  & \\
y - 4 & = & 2 \left(x - 3\right) & \\
y - 4 & = & 2x - 6 & \\
y& = & 2x-2\\
\end{array} \] Algebraically, we can verify that the slope is indeed $2$ and that when $x =3$ we get $y = 4$.  Using a graphing utility with a window centered at the point $(3, 4)$,  we graph both $y = 2x-1$ and $y = 2x-2$ below on the left and observe that they appear to be parallel.

\item To find the line perpendicular to $y = 2x-1$ containing $(3,4)$, we use the slope $m = -\frac{1}{2}$ in the point-slope formula:\[\begin{array}{rclr} 
y - y_0 & = & m\left(x - x_0\right)  & \\ 
y - 4 & = & -\frac{1}{2} \left(x - 3\right) & \\  
y - 4 & = & -\frac{1}{2} x + \frac{3}{2} & \\  
y& = & -\frac{1}{2} x + \frac{11}{2}\\
\end{array} \]  Algebraically, we check that the slope is $m = -\frac{1}{2}$ and when $x = 3$ we get $y = 4$ as required.  

You can use this Desmos graph to check both solutions.

\begin{center}
\desmos{3tf8anzvzp}{800}{600}
\end{center}

\end{enumerate}


\end{example}

\phantomsection

\label{inversemidpoint}

Our last example with lines sets up a fourth kind of symmetry which will be revisited in Section \ref{InverseFunctions}.  

\begin{example} \label{inversemidpointex2} Show that the points $(a,b)$ and $(b,a)$ in the $xy$-plane are symmetric about the line $y = x$.  

 

{\bf Solution.}  If $a = b$ then $(a, b) = (a, a) = (b, a)$ and this point lies on the line $y = x$.\footnote{Please ask your instructor if lying on the line counts as being `symmetric about the line' or not.}  To prove the claim for the case when $a \neq b$, we will show that the line $y = x$ is a perpendicular bisector of the line segment with endpoints $(a,b)$ and $(b,a)$, as illustrated below.

\begin{center}
\begin{tikzpicture}[scale=0.9]

% Axes
\draw[->] (-1.2,0) -- (5.2,0) node[right] {\scriptsize $x$};
\draw[->] (0,-1.2) -- (0,5.2) node[above] {\scriptsize $y$};

% Line y=x
\draw[<->, thick] (-1,-1) -- (4,4);

% Dashed segment joining the reflected points
\draw[dashed] (1,3) -- (3,1);

% Right-angle marker
\draw (1.82,2.18) -- (2,2.36) -- (2.18,2.18) -- (2,2);

% Points
\fill (1,3) circle (3pt);
\fill (3,1) circle (3pt);
\fill (2,2) circle (3pt);

% Labels
\node[above left] at (1,3) {\scriptsize $(a,b)$};
\node[below right] at (3,1) {\scriptsize $(b,a)$};

\end{tikzpicture}
\end{center}

To show the `perpendicular' part,  we first note the slope of the line containing $(a,b)$ and $(b,a)$ is \[ m= \frac{a-b}{b-a} = \frac{\cancel{(a-b)}}{-\cancel{(b-a)}} = -1\]

Since the slope of $y = x = 1x + 0$ is $m = 1$,  we see that the slopes of these two lines are negative reciprocals.  Hence, $y=x$ and the line segment with endpoints $(a,b)$ and $(b,a)$ are perpendicular.  For the `bisector' part, we use Equation \ref{midpointformula} to find the midpoint of  the line segment with endpoints $(a,b)$ and $(b,a)$:


\[ \begin{array}{rcl}

 M & = & \left( \frac{a+b}{2},  \frac{b+a}{2} \right) \\
   & = & \left( \frac{a+b}{2},  \frac{a+b}{2} \right)  \\ \end{array} \]

Since the $x$ and $y$ coordinates of this point are the same, we find that the midpoint lies on the line $y=x$. 

\end{example}

%% SKIPPED %% \input{ExercisesforAppLines}

\closegraphsfile

\end{document}
