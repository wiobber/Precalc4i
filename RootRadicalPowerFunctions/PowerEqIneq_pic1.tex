\tikzset{
\pgfplotsset{
  fplot/.style={
    xlabel={$x$},  % x-axis label
    ylabel={$y$},  % y-axis label	
	axis lines=middle,
	axis equal image,
	axis line style={->},
	ticks=none,
    samples=200,
    smooth,
    line width=1.25pt,
	mark=none,
	domain=-2:2,
	clip=false,
	% postaction={decorate},
    % decoration={
    %   markings,
    %   mark=at position 1 with {\arrow{>}}
    % }
  },
   fpplot/.style={
    fplot,
    variable=t,
    parametric,
   }
}
}

\begin{tikzpicture}
\begin{axis}[fplot, xlabel={}, ylabel={}, xmin=-1, xmax=3, ymin=-1, ymax=3, width=100pt, height=100pt, scale only axis]
% Axis-tip labels
\node[font=\scriptsize] at (axis cs:3,-0.5) {$L$};
\node[font=\scriptsize] at (axis cs:-0.5,3) {$K$};

% Point label
\node[font=\scriptsize] at (axis cs:2.5,1) {$(1, (228.\overline{7128})^{4})$};

% Curve
\addplot+[fplot, domain=0.7:2, <->] {1/(x^3)};

% Marked point
\addplot+[only marks, mark=*, mark size=2pt] coordinates {(1,1)};

% Caption-like annotation
\node[font=\scriptsize, anchor=north] at (rel axis cs:0.5,-0.05) {$K = f(L) =  (228.\overline{7128})^{4} L^{-3}$};
\end{axis}
\end{tikzpicture}