You are a LaTeX expert. For each LaTeX file provided:
- Convert the mfpic picture into tikzpicture,
- Use \begin{axis} when appropriate.
- Keep the original size and aspect-ratio of the mfpic for the tikzpicture
- Keep all existing formatting, commands, and structure intact.
- Ensure the output is valid LaTeX code that can compile.
- Do not add extra explanations or text outside the file contents.
- For all \begin{axis} plots, assume and use or locally overwrite an existing style with 
\tikzset{
\pgfplotsset{
  fplot/.style={
    xlabel={$x$},  % x-axis label
    ylabel={$y$},  % y-axis label	
	axis lines=middle,
	axis equal image,
	axis line style={->},
	ticks=none,
    samples=200,
    smooth,
    line width=1.25pt,
	mark=none,
	domain=-2:2,
	clip=false,
	% postaction={decorate},
    % decoration={
    %   markings,
    %   mark=at position 1 with {\arrow{>}}
    % }
  },
   fpplot/.style={
    fplot,
    variable=t,
    parametric,
   }
}
}

- Return each file in the following format:
--- File: filename ---
<file content>
--- End of file ---